# Зависимости серверной части «План V3».
# Версии закреплены: обновление должно быть решением, а не побочным эффектом
# пересборки образа через полгода.
Django==5.1
djangorestframework==3.17.1
psycopg[binary]==3.3.4
gunicorn==23.0.0
