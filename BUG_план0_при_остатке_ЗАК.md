# Баг: авто-план = 0, хотя подсказка показывает остаток «≤N (ЗАК)»

Дата: 30.07.2026. Скриншоты: `screenshots/microplan_plan0_zak_20260730.jpg`,
`screenshots/microplan_plan0_20260730.jpg`.

## Как проявляется

Микроплан, ШЦ, заказ ЗАК-0001, норма 301,84, состав 97/97, эфф. 100%.
Строки по датам подряд: план 145, 35, 143, 143, **67**, **0**, **0**;
подсказка остатка заказа при этом: ≤1151, ≤1006, ≤971, ≤828, ≤685, ≤618, ≤618.

Пользователь ставит тот же заказ на следующий день — авто-план ставит 0,
хотя подсказка говорит, что можно ещё 618 шт.

## Причина

Остаток заказа считается ДВУМЯ разными критериями:

- подсказка «≤N (ЗАК)» — `validatePlanOrderLimit` ([План.html:3319](План.html#L3319)):
  `metric: 'plan'`, т.е. кол-во заказа − **план** прошлых дней;
- сам авто-план — `_recalcPlanReleaseCore` ([План.html:8642](План.html#L8642),
  [8660](План.html#L8660), [8685](План.html#L8685), подзаказы [8756](План.html#L8756)):
  `metric: 'maxFactPlan'`, т.е. кол-во заказа − **max(факт, план)** прошлых дней.

Пока факт ≤ план, значения совпадают. Как только на прошлых днях факт превысил план,
расчёт считает заказ выбранным раньше, чем подсказка. На скриншоте расхождение
постоянное и равно 618 шт: 685 − 67 = 618, 618 − 0 = 618 — это накопленное
превышение факта над планом по этому заказу/переделу на более ранних датах.

Дополнительно: подсказка остатка для ПОДЗАКАЗА ([План.html:8464](План.html#L8464))
использует `maxFactPlan`, т.е. третий вариант — сама с main-подсказкой не согласована.

## Что решить перед правкой

Вариант A — «план не реагирует на факт» (как заявлено в комментарии
[План.html:8710](План.html#L8710) и как считает подсказка): в ядре и в подсказке
подзаказа перевести всё на `metric: 'plan'`. Тогда перевыполнение прошлых дней
не обнуляет план следующих. Риск: суммарный план может превысить кол-во заказа,
если факт уже перевыполнен.

Вариант B — физический остаток (как считает ядро сейчас): наоборот, перевести
подсказку `validatePlanOrderLimit` на `maxFactPlan` и подписывать, почему 0
(«заказ закрыт по факту»). Тогда 0 становится объяснимым, а не «багом».

Ортогонально к A/B: показывать причину нулевого плана рядом с полем
(«мощность смены занята» / «нет состава» / «остаток заказа 0»), иначе 0 всегда
читается как поломка.

## Проверка

Нужен экспорт JSON (меню «Файл») + VM-харнесс из CLAUDE.md §6: прогнать
`recalcAllMicroPlans()` до и после правки на реальных данных, сверить план
по строкам заказа ЗАК-0001.
